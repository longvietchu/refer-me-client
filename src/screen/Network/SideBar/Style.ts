import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  root: {
    borderRadius: 10,
  },
  title: {
    marginLeft: "-10px",
    color: "#00000099",
  },
  amount: {
    color: "#00000099",
  },
}));
